.payment_form
  %h4 Donating to #{@list.title} Wedding List

  =form_tag("/payment") do
    =label_tag(:amount, "How much are your giving to the couple:")
    =text_field_tag(:amount)

    %h5 Enter your credit card details below
    %section{:id=>"dropin"}
    %sub All transactions processed via Braintree Payments Sandbox
    %br
    %br
    =hidden_field_tag(:list_id, @list.id)
    =submit_tag("Pay Now")
    %br
    %br


:javascript
  braintree.setup("#{@bt_client_token}", 'dropin', {
    container: 'dropin'
  });
